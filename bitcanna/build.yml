version: '3.4'

services:
  node:
    build:
      context: ../
      args:
        PROJECT: bitcanna
        PROJECT_BIN: bcnad
        PROJECT_DIR: .bcna
        VERSION: v3.1.1
        REPOSITORY: https://github.com/BitCannaGlobal/bcna
        GOLANG_VERSION: 1.21-bullseye
        DEBIAN_VERSION: bullseye
    ports:
      - '26656:26656'
      - '26657:26657'
      - '1317:1317'
    volumes:
      - ./node-data:/root/.bcna
