services:
  node:
    build:
      context: ../
      args:
        PROJECT: stride
        PROJECT_BIN: strided
        VERSION: v29.1.1
        REPOSITORY: https://github.com/Stride-Labs/stride
        PROJECT_DIR: .stride
        GOLANG_VERSION: 1.23
        DEBIAN_VERSION: bookworm
    ports:
      - '26656:26656'
      - '26657:26657'
      - '1317:1317'
    volumes:
      - ./node-data:/root/.stride
